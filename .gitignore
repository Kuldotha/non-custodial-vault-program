# Build output
target/
.anchor/
test-ledger/
node_modules

# Operational tooling, deliberately not published: it is tied to this deployment's
# devnet state, and .test-wallets.json inside it holds funded secret keys.
scripts/
*.pem
id.json
**/deploy/*-keypair.json

# Editor / OS
.idea/
.vscode/
.DS_Store
